# Windows系统重装辅助工具｜文档索引
> 技术栈：**Tauri + Vue3 + Rust**；目标平台：Windows；包含核心重装辅助 + 3项扩展常驻功能；模块化分阶段开发

## 文档总览
本套文档完整包含：需求定义、技术可行性、项目目录结构、Rust后端数据模型/服务/Tauri命令、Vue3前端设计、性能轻量化方案、分阶段可执行开发任务。

---

## 一、需求与产品定义
### 1.核心功能：软件路径记录与目录管理
1. 扫描已安装软件（含编程语言运行时），记录名称、原始路径、版本、官方下载链接
2. 路径编辑：原始路径 / 自定义路径 二选一
3. 重装系统后：一键批量创建目录、清理本工具生成的空目录
4. 编程语言环境变量辅助：生成bat/PowerShell脚本 / 调用API设置`JAVA_HOME`/`NODE_HOME`/`PYTHON_HOME`

### 2.扩展常驻功能

|模块|功能简述|常驻属性|
|---|---|---|
|功能1｜输入法智能管理|窗口焦点识别，代码文件默认英文；区分左右Shift按键行为；优先第三方输入法；zone状态逻辑|常驻后台，可开关|
|功能2｜桌面美化|≤100MB图片静态壁纸、≤100MB视频动态壁纸；任务栏透明、关闭搜索框、任务栏图标居中；Win11切换Win7传统开始菜单；菜单自定义|壁纸/任务栏模块常驻；菜单修改一次性|
|功能3｜启动项管理|扫描开机自启软件、Windows系统服务、计划任务；图形化禁用开关；关键服务安全警告|按需打开，非常驻|

### 全局约束
1. 不携带任何软件安装包，仅提供下载链接
2. 常驻模块内存＜50MB，空闲CPU≈0
3. 模块化，常驻功能独立开关
4. 小白向UI；单文件Tauri程序

---

## 二、项目目录结构索引
### 1）前端 Vue3(src/)

```md
src
├── api            # Tauri 命令调用封装
├── assets         # 样式、静态资源 (scss 变量、混入)
├── components     # 通用组件、软件表格、路径编辑器、布局组件
├── router         # 路由配置
├── stores (Pinia)  # 软件状态、配置状态、UI 状态
├── types          # TS 类型定义，和 Rust 结构体对齐
└── views          # 页面视图
├─ScannerView.vue        软件扫描 & 路径编辑
├─DirectoryManagerView.vue 目录创建删除
├─EnvVarHelperView.vue    环境变量脚本生成
├─SettingsView.vue       设置页
└─（待新增）输入法、美化、启动项管理页面

```

### 2）后端 Rust(src‑tauri/src/)

```md
src‑tauri/src
├── main.rs            # Tauri 入口，注册全部命令
├── commands           # Tauri 命令层，接收前端调用
├── models             # 数据模型定义 (software.rs)
├── services           # 业务逻辑服务层（与命令解耦，便于单元测试）
│   ├─scanner.rs       # 软件扫描
│   ├─config_service.rs #配置读写导入导出
│   ├─directory_service.rs #目录创建删除
│   ├─env_service.rs    #环境变量脚本生成
│   ├─input_manager.rs  #【待开发】输入法管理服务
│   ├─wallpaper_service.rs #【待开发】壁纸美化服务
│   └─startup_manager.rs #【待开发】启动项管理服务
└── utils              #工具函数：注册表、快捷方式、路径处理

```

### 3）依赖清单
- 前端：vue3、vue‑router、pinia、element‑plus、@tauri‑apps/api、sass；unplugin‑vue‑components自动按需引入
- Rust：tauri、serde、serde_json、winreg、dirs、thiserror、anyhow
- release编译配置：LTO、opt‑level="z"、strip符号、panic=abort

---

## 三、后端核心数据结构索引（models/software.rs）
1. `SoftwareInfo`：扫描得到原始软件信息
2. `EnvVar`：编程语言环境变量定义（key、value_suffix、add_to_path）
3. `SoftwareItem`：UI可编辑条目；包含`original_path`、`custom_path`、`use_custom:bool`；提供`effective_path()`获取生效路径
4. `AppConfig`：全局配置文件，`software_list`软件列表 + `created_dirs`本工具创建的目录记录
5. `DirectoryOpResult`：目录操作返回结构体，succeeded/failed/skipped

> 配套：types/ 下TS接口与Rust结构体一一对应，保障序列化一致

## 四、Tauri全部命令索引
|命令名|模块|输入参数|返回值|功能|
|---|---|---|---|---|
|scan_software|scanner|无|`Vec<SoftwareItem>`|后台线程扫描已安装软件|
|get_config|config|无|`AppConfig`|读取默认路径配置|
|save_config|config|config:AppConfig|Result|保存配置到默认json|
|export_config|config|config+path字符串|Result|导出配置到用户选择路径|
|import_config|config|path字符串|`AppConfig`|从外部json导入配置|
|create_directories|directory|config:AppConfig|`DirectoryOpResult`|批量创建目录，自动更新配置内created_dirs|
|delete_created_directories|directory|config:AppConfig|`DirectoryOpResult`|仅删除工具创建的空目录，更新配置|
|generate_env_script|env_var|config+script输出路径|Result|生成.bat环境变量脚本|

> 扩展功能后续新增命令：输入法启停、壁纸设置、任务栏美化、启动项获取/禁用等

---

## 五、前端页面与组件索引
### 页面
1. **ScannerView.vue**：扫描软件，编辑自定义路径，保存/导入/导出配置；核心组件`SoftwareTable.vue`、`PathEditor.vue`
2. **DirectoryManagerView.vue**：目录创建、删除；展示DirectoryOpResult执行结果
3. **EnvVarHelperView.vue**：展示运行时环境变量，生成bat脚本
4. SettingsView：基础设置；预留扩展入口

### 复用组件
1. SoftwareTable.vue：软件列表表格
2. PathEditor.vue：单元格路径编辑组件（原始/自定义路径单选切换）
3. DirectoryOpResult.vue：目录操作结果展示组件

### Pinia Store
1. useSoftwareStore：软件列表、加载状态、configDirty标记、buildConfig组装完整配置对象

---

## 六、性能&轻量化设计要点索引
1. 阻塞IO操作全部用`spawn_blocking`后台线程，不阻塞UI主线程
2. 常驻模块全部**事件驱动**，禁止轮询；可独立开关，不强制驻留
3. 扫描来源：注册表Uninstall项为主，快捷方式补充；过滤系统更新、系统组件
4. 目录删除安全：**只删本工具记录的空目录**；非空目录直接跳过
5. setx生成环境脚本；注意PATH长度限制
6. Release编译优化，减小二进制体积；按需引入前端组件，路由懒加载
7. 资源约束：常驻内存<50MB，空闲CPU≈0

---

## 七、分阶段开发任务索引（可直接作为todo清单）

### 阶段0｜项目初始化环境搭建
### 阶段1｜核心功能：定义数据模型、Tauri命令骨架，前后端通信调通

### 阶段2｜核心功能：实现注册表扫描、软件识别、编程语言识别逻辑

### 阶段3｜核心功能：配置文件读写，导入导出
### 阶段4｜核心功能：目录创建与删除逻辑

### 阶段5｜核心功能：环境变量bat脚本生成
### 阶段6｜核心功能：前端页面完整交互
### 阶段7｜核心功能：集成测试，修复bug
### 阶段8｜功能1｜输入法智能管理（Windows钩子、焦点监听、左右Shift逻辑）
### 阶段9｜功能2｜桌面美化：静态/动态壁纸、任务栏、传统开始菜单切换
### 阶段10｜功能3｜启动项管理：自启软件、服务、计划任务枚举&禁用
### 阶段11｜整体全量测试、性能调优、打包发布

---

## 八、风险点索引（文档内标注难点）
1. 任务栏图标自主居中实现难度高，存在Explorer注入风险；备选方案集成TaskbarX
2. Tauri命令传参为值拷贝；修改`AppConfig.created_dirs`后需要写回持久化配置
3. setx命令存在PATH长度上限
4. 输入法模块Windows IMM / TSF接口API复杂，低级键盘钩子稳定性需要测试
5. 动态视频壁纸需要操作WorkerW桌面窗口，解码资源开销控制
6. 系统服务操作需要管理员权限；系统关键服务要增加保护告警
7. 路径含中文、空格、特殊盘符缺失场景需要容错处理

